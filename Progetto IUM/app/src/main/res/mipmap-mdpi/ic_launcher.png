package com.example.bonus;

import androidx.appcompat.app.AppCompatActivity;

import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.EditText;
import android.widget.TextView;

import java.io.Serializable;
import java.util.ArrayList;


public class MainActivity extends AppCompatActivity {


    //private Utente utente1 = new Utente("Andrea","1234","Cagliari", "10/09/97");

    EditText username, password;
    Button button_login;
    TextView registrazione,errors;
    String actualUsername,actualPassword;
    Utente actualUser = null;
    public static final String UTENTE_EXTRA="com.example.andrea.bonusIum.Utente";
    ArrayList<Utente> databaseUtenti = new ArrayList<Utente>();


    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);



        Intent intent = getIntent();
        Serializable obj = intent.getSerializableExtra(MainActivity.UTENTE_EXTRA);

        if(obj!=null){
            databaseUtenti.add((Utente)obj);
        }

        username =findViewById(R.id.testo1);
        password = findViewById(R.id.password);
        button_login = findViewById(R.id.button_login);
        registrazione = findViewById(R.id.registrazione);
        errors = findViewById(R.id.erroreLogin);

        button_login.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {

                if (checkInput())
                {

                    //prelievo dei valori inseriti negli EditText
                    actualUsername = username.getText().toString();
                    actualPassword = password.getText().toString();

                    for (Utente u : databaseUtenti) {

                        if (actualUsername.equals(u.getUsername()) &&
                                actualPassword.equals(u.getPassword())) {
                